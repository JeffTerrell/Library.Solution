@{
  Layout = "_Layout";
}

@model Library.Models.Patron

@using (Html.BeginForm())
{
  @Html.LabelFor(patron => patron.Name)
  @Html.TextBoxFor(patron => patron.Name, new {@required = "required"})
  <input type="submit", value="Add patron", class="btn btn-dark"/>
}